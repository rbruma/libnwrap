This directory contains an implementation of the POSIX.1g 
getaddrinfo() function.

Known bugs / limitations at this stage :

1. resolver process for both AF_INET & AF_INET6 doesn't work
2. gethostbyname_r is inefficient
